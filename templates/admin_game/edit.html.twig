{% extends 'base.html.twig' %}

{% block title %}✏️ Edit {{ game.title }} | Admin{% endblock %}

{% block body %}
<div class="min-h-screen bg-[#0f172a] py-12 px-6">

    <div class="max-w-3xl mx-auto">

        <div class="bg-[#111b2c] border border-white/10 rounded-2xl shadow-2xl p-8">

            <h1 class="text-2xl font-bold text-white mb-8 flex items-center gap-2">
                ✏️ Edit Game
            </h1>

            {{ form_start(form) }}

            {# TITLE #}
            <div class="mb-6">
                <label class="block text-sm font-semibold text-gray-300 mb-2">
                    Game Title
                </label>
                {{ form_widget(form.title, {
                    attr: {
                        class: 'w-full bg-[#0f172a] border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-blue-500 transition'
                    }
                }) }}
            </div>

            {# DESCRIPTION #}
            <div class="mb-6">
                <label class="block text-sm font-semibold text-gray-300 mb-2">
                    Game Description
                </label>
                {{ form_widget(form.description, {
                    attr: {
                        class: 'w-full bg-[#0f172a] border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-blue-500 transition',
                        rows: 5
                    }
                }) }}
            </div>

            {# PRICE #}
            <div class="mb-6">
                <label class="block text-sm font-semibold text-gray-300 mb-2">
                    Price (€)
                </label>
                {{ form_widget(form.price, {
                    attr: {
                        class: 'w-full bg-[#0f172a] border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-blue-500 transition'
                    }
                }) }}
            </div>

            {# ✅ RELEASE YEAR VERSION COMPACTE #}
            <div class="mb-6">
                <label class="block text-sm font-semibold text-gray-300 mb-2">
                    Release Year
                </label>

                <div class="bg-white/5 border border-white/10 rounded-xl px-4 py-3 hover:bg-white/10 transition duration-300">
                    <div class="flex items-center gap-3">

                        <div class="bg-blue-600/20 p-2 rounded-lg">
                            <!-- Icône calendrier plus clean -->
                            <svg xmlns="http://www.w3.org/2000/svg"
                                 class="w-5 h-5 text-blue-400"
                                 fill="none"
                                 viewBox="0 0 24 24"
                                 stroke="currentColor">
                                <path stroke-linecap="round"
                                      stroke-linejoin="round"
                                      stroke-width="2"
                                      d="M6 2v2M18 2v2M3 8h18M5 6h14a2 2 0 012 2v12a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2z"/>
                            </svg>
                        </div>

                        {{ form_widget(form.releaseYear, {
                            attr: {
                                class: 'w-full bg-transparent text-white focus:outline-none text-sm'
                            }
                        }) }}

                    </div>
                </div>
            </div>

            {# CATEGORY #}
            <div class="mb-6">
                <label class="block text-sm font-semibold text-gray-300 mb-2">
                    Category
                </label>
                {{ form_widget(form.category, {
                    attr: {
                        class: 'w-full bg-[#0f172a] border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-blue-500 transition'
                    }
                }) }}
            </div>

            {# IMAGE UPLOAD #}
            <div class="mb-4">
                <label class="block text-sm font-semibold text-gray-300 mb-2">
                    Game Image (JPG / PNG / WebP)
                </label>

                <div class="relative border-2 border-dashed border-white/20 rounded-xl p-6 text-center bg-[#0f172a] hover:border-blue-500 transition cursor-pointer">

                    {{ form_widget(form.imageFile, {
                        attr: {
                            class: 'absolute inset-0 w-full h-full opacity-0 cursor-pointer'
                        }
                    }) }}

                    <div class="text-gray-400 text-sm">
                        📁 Click to upload or drag & drop
                    </div>

                    <div class="text-xs text-gray-500 mt-2">
                        JPG, PNG or WebP
                    </div>
                </div>
            </div>

            {% if game.image %}
                <div class="mb-8">
                    <div class="text-xs text-gray-400 mb-2">
                        Current image
                    </div>
                    <img src="{{ asset('images/games/' ~ game.image) }}"
                         alt="{{ game.title }}"
                         class="rounded-xl border border-white/10 w-full max-h-64 object-cover">
                </div>
            {% endif %}

            <button class="w-full bg-blue-600 hover:bg-blue-700 text-white py-3 rounded-xl font-semibold transition shadow-lg hover:shadow-blue-500/20 mb-6">
                💾 Update Game
            </button>

            {{ form_end(form) }}

            <form method="post"
                  action="{{ path('app_admin_game_delete', { id: game.id }) }}"
                  class="delete-form">

                <input type="hidden"
                       name="_token"
                       value="{{ csrf_token('delete' ~ game.id) }}">

                <button class="w-full bg-red-600 hover:bg-red-700 text-white py-3 rounded-xl font-semibold transition shadow-lg hover:shadow-red-500/20">
                    🗑 Delete Game
                </button>
            </form>

        </div>

    </div>
</div>
{% endblock %}